export type CardNavigationInfos = {
    title: string;
    description: string;
    path: string;
    iconUrl: string;
  };

  export type CardInfos = {
    title: string;
    subTitle?: string;
    description: string;
    publicImage?: string;
    isReversed?: boolean;
  };